<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">

</head>

<body>
    <div class="d-flex justify-content-between align-items-center bg-primary p-4 text-white">
        <h1 class="">Item List</h1>
        <a href="/add" class="btn btn-success rounded-0 btn-sm">Add Item</a>
    </div>
    
    <div class="  d-flex justify-content-center align-items-center">
        <form class=" d-flex my-3 w-75" method="GET" action="/">
            <input type="text" name="search" class="form-control me-2" placeholder="Search..." value="<%= search %>">
            <button class="btn btn-outline-primary btn-sm" type="submit">Search</button>
        </form>
    </div>
    
    <div class="d-flex justify-content-center align-items-center my-3">
        <ul class="list-group w-75">
            <% items.forEach((item, index) => { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                 <div>
                    <strong><%= (page - 1) * 5 + index + 1 %>.</strong>
                    <%= item.name %> - <%= item.description %>
                 </div>
                    <div>
                        <a href="/edit/<%= item._id %>" class="mx-3 btn btn-primary rounded-0 btn-sm">Edit</a>
                        <a href="/delete/<%= item._id %>" class="btn btn-danger rounded-0 btn-sm">Delete</a>
                    </div>
                </li>
            <% }) %>
        </ul>
    </div>
    
    <!-- Pagination -->
    <div class="d-flex justify-content-center">
        <nav>
            <ul class="pagination">
                <% for(let i = 1; i <= totalPages; i++) { %>
                    <li class="page-item <%= page === i ? 'active' : '' %>">
                        <a class="page-link" href="/?page=<%= i %>"><%= i %></a>
                    </li>
                <% } %>
            </ul>
        </nav>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO"
        crossorigin="anonymous"></script>

</body>

</html>